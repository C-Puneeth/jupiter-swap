# Force npm to install optional dependencies
optional=true
# Ensure platform-specific binaries are installed
target_platform=linux
target_arch=x64
# Disable package-lock for this installation
package-lock=false